version: 2

models:
  - name: int_localbike__products
    description: '{{ doc("int_localbike__products") }}'
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each order."
        tests:
          - unique
          - not_null